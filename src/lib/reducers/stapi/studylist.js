const init = {
  studies: [],
  range: {
    start: 0,
    end: 0,
    max: 0
  }
}
const reducer = (state = init, action) => {
  switch(action.type){
    default:
      return state
  }
}

export default reducer
